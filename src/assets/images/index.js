const images = {
    // File svg nen can` .default, nhung file khac khong can`
    logo: require('@/assets/images/logo.svg').default,
    noImage: require('@/assets/images/nophoto.png'),
};

export default images;
